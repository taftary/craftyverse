[package]
name = "planet-crafter-examples"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
glam = "0.33.6"
planet-crafter-engine = { path = "../../../crates/engine" }

[features]
default = []
# Enables examples that require a Vulkan-capable GPU and a display.
gpu = []

[[example]]
name = "viewer"
required-features = ["gpu"]
